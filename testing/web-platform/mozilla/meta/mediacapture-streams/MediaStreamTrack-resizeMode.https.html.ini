[MediaStreamTrack-resizeMode.https.html]
  prefs: [media.getusermedia.camera.fake.force:true, media.navigator.streams.fake:false]
  [gDM gets expected mode by true]
    expected:
      if os == "android": FAIL
  [gDM gets expected mode by {"height":100}]
    expected:
      if os == "android": FAIL
  [gDM gets expected mode by {"frameRate":5}]
    expected:
      if os == "android": FAIL
