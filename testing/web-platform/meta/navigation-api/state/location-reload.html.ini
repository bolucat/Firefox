[location-reload.html?method=updateCurrentEntry]
  [entry.getState() after location.reload()]
    expected: FAIL


[location-reload.html?method=navigate]
  expected:
    if (os == "linux") and not debug and (processor == "x86_64") and not tsan and asan and fission: [CRASH, OK]
    if (os == "linux") and not debug and (processor == "x86_64") and not tsan and not asan: [CRASH, OK]
    if (os == "mac") and not debug: [OK, TIMEOUT, CRASH]
    if os == "android": [CRASH, OK]
    [OK, CRASH]
  [entry.getState() after location.reload()]
    expected: FAIL
