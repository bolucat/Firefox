[test-analyser-resume-after-suspended.html]
  expected:
    if (os_version == "14") and debug and sessionHistoryInParent: [OK, CRASH]
    if os_version == "18.04": [ERROR, TIMEOUT]
    if os_version == "14.70": [OK, TIMEOUT]
  [AnalyserNode resume after suspended]
    expected:
      if os_version == "18.04": [TIMEOUT, PASS]
      if os_version == "14.70": [PASS, TIMEOUT]
