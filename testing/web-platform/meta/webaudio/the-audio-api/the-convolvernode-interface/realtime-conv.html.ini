[realtime-conv.html]
  expected:
    if (os == "mac") and debug: [OK, TIMEOUT]
    if (os == "mac") and not debug: [OK, TIMEOUT]
  [Test convolver with realâ€‘time context]
    expected:
      if (os == "mac") and debug: [PASS, TIMEOUT]
