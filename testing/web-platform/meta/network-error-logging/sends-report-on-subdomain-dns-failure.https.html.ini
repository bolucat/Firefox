[sends-report-on-subdomain-dns-failure.https.html]
  expected:
    if os_version == "7.0": [OK, TIMEOUT]
  [\n    Test that include_subdomains policies report DNS failures for subdomains\n  ]
    expected:
      if os_version == "7.0": [PASS, TIMEOUT]
