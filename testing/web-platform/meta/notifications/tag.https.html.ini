[tag.https.html]
  [Opening two notifications with the same tag should fire close event before show event]
    expected:
      if (os_version == "14") and sessionHistoryInParent and not debug: [PASS, FAIL]
      if (os_version == "14") and not sessionHistoryInParent and debug: [PASS, FAIL]
