[pattern-transform-03.svg]
  disabled:
    if useDrawSnapshot and tsan: unknown failure
  expected:
    if (os_version == "14") and debug and not swgl: [PASS, ERROR]
    if (os_version == "14") and not debug: [PASS, ERROR]
