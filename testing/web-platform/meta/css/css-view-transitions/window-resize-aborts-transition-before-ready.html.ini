[window-resize-aborts-transition-before-ready.html]
  expected:
    if (os == "linux") and (processor == "x86_64") and not debug and not asan and not tsan: [OK, TIMEOUT]
    if (os == "linux") and (processor == "x86"): [OK, TIMEOUT]
  [\nView transitions: Resizing viewport before animating rejects the ready promise.\n]
    expected:
      if (os == "linux") and (processor == "x86_64") and fission and not debug and not asan and not tsan: [PASS, TIMEOUT]
      if (os == "linux") and (processor == "x86_64") and not fission and not debug: [PASS, FAIL]
      if (os == "linux") and (processor == "x86"): [PASS, TIMEOUT]
      if os == "android": FAIL
