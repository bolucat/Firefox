[RTCPeerConnection-remote-track-mute.https.html]
  prefs: [media.peerconnection.mute_on_bye_or_timeout:true]
  expected:
    if (os == "linux") and not asan and not debug and (processor == "x86"): [OK, TIMEOUT]
    if (os == "linux") and asan and fission: [OK, TIMEOUT]
    if (os == "linux") and not asan and debug: [OK, TIMEOUT]
  [pc.close() on one side causes mute events on the other]
    expected:
      if (os == "linux") and not asan and not debug and (processor == "x86"): [PASS, TIMEOUT]
      if (os == "linux") and asan and fission: [PASS, TIMEOUT]
      if (os == "linux") and not asan and debug: [PASS, TIMEOUT]
