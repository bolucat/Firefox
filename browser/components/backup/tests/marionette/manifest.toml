[DEFAULT]
run-if = ["buildapp == 'browser'"]
prefs = ["browser.backup.enabled=true", "browser.backup.log=true"]

["test_backup.py"]
support-files = ["http2-ca.pem"]
skip-if = ["display == 'wayland'"] # 1924781: Gnome keyring not unlocked
